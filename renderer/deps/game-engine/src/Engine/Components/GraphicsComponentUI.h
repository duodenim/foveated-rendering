#pragma once

#include "../Component.h"
/**
 * Contains common methods for drawing UI objects on top of everything
 */
/*const uint8_t MIN_Z_LAYER = 0;
const uint8_t MAX_Z_LAYER = 50;
class GraphicsComponentUI : public Component {
public:
  GraphicsComponentUI();
  const Vec2 GetPosition() const;
  const float GetRotation() const;
  const Vec2 GetScale() const;
  const uint8_t GetLayer();
  void SetLayer(const uint8_t layer);
  void SetPosition(const Vec2 position);
  void SetRotation(const float rotation);
  void SetScale(const Vec2 scale);
protected:
  void AdjustLayering();
private:
  uint8_t mLayer; //Used to determine what UI elements to draw on top of others, like z-index in CSS
                  //0 = bottom layer, 50 = top layer

  float mScreenScale; //Used to scale images correctly on different aspect ratios. This will be used
                      //to stretch/shrink horizontally, so vertical scale will always be 1

  
};*/

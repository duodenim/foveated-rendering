#ifndef OPTIONWINDOWCOMPONENT_H
#define OPTIONWINDOWCOMPONENT_H
#include <Component.h>
#include <Components/TextComponent.h>
#include <Components/SpriteComponent.h>
#include <Components/AudioSourceComponent.h>

/*enum class CursorDirection {
  UP,
  DOWN,
  LEFT,
  RIGHT
};

class OptionWindowComponent : public Component {
public:
  OptionWindowComponent();
  void SetNumColumns(const int& columns);
  void SetCenter(const Vec2 &center);
  void AddOption(const std::string &text);
  void SetBaseLayer(const uint8_t layer);
  void MoveCursor(const CursorDirection direction);
  void Clear();*/

  /**
   * @return Text currently selected by the cursor
   */
  /*const std::string Select();
  void Draw(const float deltaTime);
private:
  int mNumColumns;
  float mColumnHeight;
  float mColumnWidth;
  float mTextScale;
  int mCursorPosition;
  std::vector<TextComponent> mOptions;
  SpriteComponent mBackground;
  SpriteComponent mCursor;
  AudioSourceComponent mSelectSound;
  AudioSourceComponent mCursorMoveSound;
  Vec2 mCenter;
};*/


#endif //OPTIONWINDOWCOMPONENT_H
